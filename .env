# 1. 服务与模型配置
# 回调基础地址
CALLBACK_BASE_URL=http://121.48.162.151:18000
# 模型路径 (必须修改为服务器上的实际路径)
MODEL_PATH=/home/xieqiuhao/multimodel_relation-main/InternVL3_5-14B-Instruct

# 2. 多GPU与性能配置
# 指定可见的GPU (例如: 0,1,2) - 建议在启动命令前 export
# CUDA_VISIBLE_DEVICES=0,1,2,3,4
# HF多卡分片策略 (auto, balanced, balanced_low_0)
HF_DEVICE_MAP=balanced
# 每张GPU的显存占用比例 (0.0 -> 1.0)
GPU_MEM_FRACTION=1.0

# 3. 算法推理参数
IMG_INPUT_SIZE=448
RGB_MAX_BLOCKS=8
IR_MAX_BLOCKS=6
MAX_NEW_TOKENS=1024
# 流式输出超时
STREAM_TIMEOUT=2.0

# 4. 服务配置
HOST=0.0.0.0
PORT=8102
# 本地开发设为 True, 生产环境 (Docker) 设为 False
RELOAD=True

# 5. 数据持久化路径
# 会话缓存目录
SESSIONS_DIR=session_cache
# CSV日志与结果目录 (./ 代表当前目录)
DATA_DIR=./